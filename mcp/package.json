{
  "name": "sadday-mcp",
  "version": "1.0.0",
  "description": "MCP server para el Club de Montaña Sadday — consultas de rutas, salidas, informes y actas",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "bundle": "esbuild src/index.ts --bundle --platform=node --outfile=dist/bundle.cjs --format=cjs",
    "package": "npm run bundle && pkg dist/bundle.cjs --targets node20-macos-arm64,node20-macos-x64,node20-win-x64,node20-linux-x64 --out-path bin --output sadday-mcp"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "axios": "^1.7.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "@yao-pkg/pkg": "^6.20.0",
    "esbuild": "^0.25.0",
    "typescript": "^6.0.3"
  }
}
